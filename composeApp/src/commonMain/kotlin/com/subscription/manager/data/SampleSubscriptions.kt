package com.subscription.manager.data

import com.subscription.manager.model.*

object SampleSubscriptions {
    val initialSubscriptions: List<Subscription> = emptyList()
    val initialPaymentHistory: List<PaymentHistoryItem> = emptyList()
    val weeklySpending: List<DaySpending> = emptyList()
}
